let initialState = {
	mainPage: {
		friends: [
			{id: 1, name: 'Sasha'},
			{id: 2, name: 'Maksim'},
			{id: 4, name: 'Xee'},
		]
	},
};

const mainpageReducer = (state = initialState, action) => {

	return state;
}

export default mainpageReducer();